# pnpm configuration for print-press-backend
# Disable workspace detection to treat this as an isolated project
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false

# Ensure pnpm doesn't look for workspace root
workspace-root=.

# Use node_modules strategy for better compatibility
node-modules=true
